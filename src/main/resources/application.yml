spring:
  couchbase:
    bootstrap-hosts: http://localhost
    bucket:
      name: users
      password: password
  security:
    basic:
      enable: ignore
    ignored: /**
  jackson:
    default-property-inclusion: NON_NULL
server:
  compression:
    enabled: true
